/* Allow users in the wheel group to manage the libvirt daemon */
polkit.addRule(function(action, subject) {
  if (action.id == "org.libvirt.unix.manage" && subject.isInGroup("{{ libvirt_allow_group }}")) {
    return polkit.Result.YES;
  }
});
